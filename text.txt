java script

//-single line comment
/*   */ -multi line comment


## variables

var- can be re-declared and updated
let- can't be re-declared but can able updated
const- can't be re-declared and can't be update

## Data types

Premitive type - string,num,boolean,undefined,null
reference type - objects,Arrays,functions

##operators

Arithematic =  +    -   *   /   %   **
Assignement =  +=  -=  *=   /=  %= **=
comparison
    relational= x<5  x>5    x<=5  x>=5
    equality=   ==  ===
    string= Concatenation(+)
ternary =  [condition]?true:false
logical - AND t,t=t
            t,f=f
            f,t=f
            f,f=f
        OR t,t=t
            f,f=f
            t,f=t
            f,t=t
        NOT
            t=f,
            f=t

bitwise =[0,1] 1=true  0=flase

        8   4   2   1

    5=  0   1   0   1
    3=  0   0   1   1

     1  = 0    0   0    1

increment | decrement

conditional statements

    if(){

    }
    else if(){

    }
    else{

    }
    nested if

looping
    while loop

        while(condition){

        }

    for loop

        for (initialization value,final value,update){

        }

    do while

        do{
            //code
        }while(condition)

    nested while
    nested for

## break,continue

    break:stop the loop immediately

## funcction
    function Declaration

        function name(){
            //code
        }
    function Expression

        const functionName=fun(a,b)=>{
            //code
        }

    Arrow Function
        const functionName =(Arguments)=>{
            //code
        }

## Arrays [] - it's a special variable that can store multiple values and it can be any type of data.

    push,pop,shift,unshift,slice,splice,indexof,includes,reverse,sort


## for =>map,reduce,find,filter,findindex,fill,some,every

    time intervals
    
        settimeout

        setouttime
    
    syn fun/asyn fun
    await metho

# objects {}

    object is collection of Keys & values

##### DMO

    Document object module

    it intracts with html file


DOM tree structure

    Document
        html
            head
                title
                    "my page"
            body
                h1
                  "content
                p
                div


#node types in DOM
        element node, text nod, attributes node


## selectors

    ID selectors
    Class selectors
    Tag selectors
    Query selectors
    all query  selectors

## reading & changing content

## classname  

## classList

    .add("classname)

        para.classList.add("classname1","classname2"....)
    
    .remove("classname)

         para.classList.remove("classname1","classname2"....)

    .contains("classname")
         console.log( para.classList.contains("classname1"))


## event handelers

syntax

        elementname.addEventListerner("eventType",callbackFuction,useCapture)

        elementname.removeEventListerner("eventType",callback)

        event types=
                    click,doubleclick,mouseover,mouseout,mouseDown,mouseup,load,scroll
        form event
                submit,change,input,blur,focus

## form and user input in js
    Reading form values
    required field with condition